# proto 文件目录
PROTO_DIR = D:/leaf/protobuf

# pb 输出根目录（go_package 中 module 前缀之后的路径会追加到此目录）
# 例如 go_package = "goAcotr/pb/game" → 输出到 ROOT_DIR/pb/game/
ROOT_DIR  = D:/leaf

# Go 模块名（与 go.mod 中的 module 一致）
MODULE    = goAcotr
