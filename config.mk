# proto 文件目录
PROTO_DIR = D:/gosrc/gogs/protocol

# pb 输出根目录（go_package 中 module 前缀之后的路径会追加到此目录）
ROOT_DIR  = D:/gosrc/gogs

# Go 模块名（与 go.mod 中的 module 一致）
MODULE    = github.com/gogu-x/gogs

# pb 输出根目录（go_package 中 module 前缀之后的路径会追加到此目录）
TOOLS_DIR  = D:/gosrc/gogs/tools