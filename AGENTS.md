# AI 开发约束

## Go 后端代码

- 修改或新增本仓库任何 Go 代码前，先阅读并遵守 [`.agent_notes/gen_code_rules_enforcement.md`](.agent_notes/gen_code_rules_enforcement.md) 中的全部规范。该文件是代码规范的唯一详细规则来源，包含 17 条编码要求、代码生成标记要求和提交结果前的自查要求。
- 这些规则适用于 `game/`、`battle/`、`gate/`、`platform/`、`rpc/` 等后端目录，也适用于为后端功能修改的其他 Go 包。
- 完成代码修改后，按该清单逐条自查。若确实需要因兼容性或现有接口而例外，必须在交付说明中指出具体规则、涉及代码和原因，不得静默忽略。
- 不要只为满足规则而改变对外协议、持久化格式或现有业务语义；遇到冲突时保留兼容性，并说明例外。

## 业务边界

- `ctl` 写接口文件只负责客户端交互入口（如 `Handle`）：解析/校验请求、调用 logic、组装并返回响应。不要把完整业务流程或所有方法堆在 `ctl` 文件中；业务规则和数据变更放到对应的独立 `logic` 文件/模块。
- 业务逻辑读取和修改数据时，直接操作已经加载到内存的数据结构。不要在接口请求或普通业务逻辑中为读取数据、统计数量而直接查询数据库；数据库访问集中在数据加载和存盘职责中。
- 持久化数据按生命周期加载：公共数据在服务器启动初始化时重载，玩家数据在玩家登录时重载。运行期间由内存数据结构承载业务读写；通过定时存盘、关服存盘和玩家离线存盘将变更持久化。不要在每次接口调用中临时重载数据或直接落库。
- 开始修改前先查阅目标模块及其调用方，沿用现有模块职责、Actor/协程通信边界和数据访问方式，不要未经确认跨模块搬移职责。
- 涉及战斗链路时，先阅读 [`battle/BATTLE_BACKEND.md`](battle/BATTLE_BACKEND.md)，并以实际代码和协议定义为准；文档描述与代码不一致时，指出差异后再实现。
- 修改协议、配置或生成代码时，先确认对应源文件和生成流程；不要把手工修改生成产物当作唯一实现。
